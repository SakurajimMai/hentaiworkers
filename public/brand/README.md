# AnimeStream brand assets

`animestream-mark.svg` is the transparent master mark. `animestream-icon.svg` is the
framed master icon. Run `npm run brand:assets` after editing either SVG to regenerate
the web install icons, Apple touch icon, and legacy Android launcher WebP files.
