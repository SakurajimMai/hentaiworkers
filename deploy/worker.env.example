# Worker container only. Create the node in /admin/crawler/workers first.
# The token is shown once; never place App or database secrets in this file.
CRAWLER_WORKER_ID=
CRAWLER_WORKER_TOKEN=
CRAWLER_WORKER_VERSION=1.0.0
