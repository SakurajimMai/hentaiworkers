# Absolute HTTP(S) origin of the Next app (no path/query/hash).
# Required for native builds; web uses same-origin relative /api.
EXPO_PUBLIC_API_BASE_URL=https://www.ixacg.de

# Optional Metro /api proxy override during local Expo web/dev.
# MOBILE_DEV_API_BASE_URL=
